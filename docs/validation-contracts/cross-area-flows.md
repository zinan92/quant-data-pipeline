## Cross-Area Flows

### VAL-CROSS-001: Health Dashboard Shows Zero Gaps for Backfilled Date Range
After the historical backfill (milestone 2) completes for all stocks and indices, the gap detection feature (milestone 3) must report zero gaps for the backfilled period (2021-01-04 through the latest backfill date). Pass: `GET /api/health/gaps` returns a response where every symbol in the expanded universe has `gap_count = 0` (or ≤ 5 gaps explainable by suspensions) for the date range 2021-01-04 to the latest trading day. The health dashboard UI's gap detection section shows "no gaps found" or a near-zero summary. Fail: the gap detection endpoint reports > 5 unexplained gaps for any stock, or > 2 gaps for any index, within the backfilled range.
Evidence: After backfill completion, call `GET /api/health/gaps` and verify the response. Cross-reference with `SELECT symbol_code, COUNT(*) AS gaps FROM (SELECT date FROM trade_calendar WHERE is_trading_day=1 AND date >= '2021-01-04' EXCEPT SELECT trade_time FROM klines WHERE symbol_type='stock' AND symbol_code=<code> AND timeframe='DAY') GROUP BY symbol_code HAVING gaps > 5;` — must return empty (excluding recently-listed or suspended stocks). Use agent-browser to verify the health page's gap detection section visually shows clean status.

### VAL-CROSS-002: Expanded Stock Universe Is Fully Backfilled
Every stock added by the stock universe expansion (milestone 1, VAL-INFRA-007) must have 5-year daily kline data after the backfill (milestone 2). Pass: `SELECT w.ticker FROM watchlist w LEFT JOIN (SELECT DISTINCT symbol_code FROM klines WHERE symbol_type='stock' AND timeframe='DAY' AND trade_time <= '2021-06-01') k ON w.ticker = k.symbol_code WHERE k.symbol_code IS NULL;` returns only stocks listed after 2021-01-01. Every ticker in the expanded watchlist (≥ 800 symbols) has corresponding kline data reaching back to 2021. Fail: more than 50 watchlist tickers that were listed before 2021 have no kline data before 2022, indicating the backfill missed the expanded universe.
Evidence: Query `SELECT COUNT(*) FROM watchlist w WHERE NOT EXISTS (SELECT 1 FROM klines k WHERE k.symbol_code = w.ticker AND k.symbol_type='stock' AND k.timeframe='DAY' AND k.trade_time < '2021-06-01');` and cross-reference against `list_date` from `stock_basic` to exclude post-2021 IPOs. The remaining count (stocks with no early data despite pre-2021 listing) must be ≤ 10. Also verify the backfill script sources its symbol list from the watchlist table (post-expansion), not from a hardcoded list.

### VAL-CROSS-003: New Indices Have 5-Year Daily Data After Backfill
The three new indices added in milestone 1 (000300.SH/CSI 300, 000905.SH/CSI 500, 000852.SH/CSI 1000 — VAL-INFRA-005) must have complete 5-year daily kline data after the backfill (milestone 2, VAL-BACKFILL-004). Pass: for each of the three new index codes, `SELECT MIN(trade_time), MAX(trade_time), COUNT(*) FROM klines WHERE symbol_type='index' AND symbol_code IN ('000300.SH','000905.SH','000852.SH') AND timeframe='DAY' GROUP BY symbol_code;` returns min_date ≤ '2021-01-08' and count ≥ 1,100 for each. Fail: any new index has fewer than 500 rows or a start date later than 2022-01-01.
Evidence: Run the SQL query above against `data/market.db` after backfill. Verify the backfill script iterates over the full `INDEX_LIST` (which must include the 3 new entries from milestone 1) rather than a separate hardcoded list. Cross-check that `src/services/index_updater.py`'s `INDEX_LIST` has 8 entries and the backfill uses the same list or its superset.

### VAL-CROSS-004: Cleanup Does Not Delete Backfilled Historical Data
After the backfill (milestone 2) populates 5 years of data, the cleanup job (modified in milestone 1 to retain 1825 days for daily klines — VAL-INFRA-001) must not delete any of the backfilled data. Pass: after running `cleanup_old_klines()` post-backfill, `SELECT COUNT(*) FROM klines WHERE symbol_type='stock' AND timeframe='DAY' AND trade_time >= '2021-01-04'` returns the same count as before cleanup. No daily kline row within the 5-year window is deleted. Fail: running cleanup after backfill reduces the daily kline count for dates within the last 1825 days (≈5 years).
Evidence: Record `SELECT COUNT(*) FROM klines WHERE timeframe='DAY';` before and after running the cleanup job. Verify the count is unchanged (or only decreased by rows older than 1825 days, which should not exist if backfill starts at 2021). Specifically confirm: `SELECT COUNT(*) FROM klines WHERE timeframe='DAY' AND trade_time < date('now', '-1825 days');` returns 0 after cleanup — no daily data older than the retention window exists, and no data within the window was touched. Also verify `calendar_updater.py` → `cleanup_old_klines()` uses `days=1825` for `KlineTimeframe.DAY` (not the old `days=365`).

### VAL-CROSS-005: Trade Calendar Extended Range Powers Gap Detection
The trade calendar extended to 2021-2026 in milestone 1 (VAL-INFRA-003) must be used by the gap detection logic in milestone 3 (VAL-HEALTH-002) to identify missing kline data across the full 5-year period. Pass: the gap detection endpoint's query joins against `trade_calendar` with dates starting from 2021-01-04 (not just the current year). If a stock has a kline gap on a 2021 trading day, the gap endpoint reports it. Fail: gap detection only checks the current year's calendar entries (the pre-expansion behavior), ignoring 2021-2024 dates entirely.
Evidence: Inspect the gap detection endpoint's SQL query to verify it does not filter `trade_calendar` by `date >= '<current_year>0101'`. Test by temporarily deleting a single kline row for a stock on a 2022 trading day and calling `GET /api/health/gaps` — the deleted date must appear in the gap results. Restore the row afterward. Also verify `SELECT MIN(date) FROM trade_calendar;` returns ≤ '2021-01-04' (confirming the calendar covers the range the gap detector needs).

### VAL-CROSS-006: Health Dashboard Accurately Reflects Backfill State
The health dashboard (milestone 3) must correctly show the freshness and completeness of backfilled data (milestone 2), including: per-stock freshness reflecting the latest kline date, gap counts matching actual missing days, and consistency checks passing for backfilled data. Pass: (a) the per-stock drill-down shows each stock's latest date as ≥ yesterday's trading day, (b) the gap detection section count matches the SQL-computed gap count, and (c) the data consistency section does not flag false positives caused by backfill artifacts (e.g., different data source during backfill vs. live updates). Fail: (a) freshness shows stale dates for stocks that were successfully backfilled, (b) gap counts differ between the dashboard and direct SQL queries by more than 10%, or (c) consistency checks flag >20% of symbols due to backfill-vs-live data format differences.
Evidence: After backfill, navigate to `/health` via agent-browser and compare displayed freshness dates against `SELECT symbol_code, MAX(trade_time) FROM klines WHERE symbol_type='stock' AND timeframe='DAY' GROUP BY symbol_code LIMIT 10;`. Verify gap counts match `GET /api/health/gaps` response. Check consistency section does not show a mass of failures caused by minor precision differences between TuShare (backfill source) and Sina/East Money (live source).

### VAL-CROSS-007: Live Scheduler Works With Expanded Universe and New Indices
After infrastructure changes (milestone 1) expand the stock universe and add new indices, the live update scheduler (`KlineScheduler._job_daily_refresh`, `_job_30m_refresh`, `_job_all_stock_daily`) must continue to function correctly with the larger dataset. Pass: a scheduled or manually-triggered daily refresh successfully updates klines for all ~1000 stocks and all 8 indices without errors. The scheduler logs show all symbols processed, and `DataUpdateLog` entries show `status='completed'`. Fail: the scheduler crashes, times out, or skips the new indices/stocks during live updates. Any `DataUpdateLog` entry with `status='failed'` for index_daily, stock_daily, or all_stock_daily after the expansion.
Evidence: Trigger a manual scheduler run via the admin API or `_job_daily_refresh()`. Check logs for errors. Query `SELECT update_type, status, records_updated, error_message FROM data_update_log ORDER BY started_at DESC LIMIT 20;` and verify all recent updates are `COMPLETED`. Confirm `SELECT DISTINCT symbol_code FROM klines WHERE symbol_type='index' AND timeframe='DAY' AND trade_time = '<today>';` returns 8 rows (all indices including the 3 new ones). For stocks: `SELECT COUNT(DISTINCT symbol_code) FROM klines WHERE symbol_type='stock' AND timeframe='DAY' AND trade_time = '<today>';` returns ≥ 800.

### VAL-CROSS-008: Quant-Dashboard Reads Backfilled Data Successfully
The `quant-dashboard` project at `/Users/wendy/work/trading-co/quant-dashboard/` accesses the same `data/market.db` via `MarketReader` in read-only mode. After backfill (milestone 2), it must be able to read the full 5-year data range for stocks and indices, including the expanded universe and new indices. Pass: `MarketReader.get_stock_klines(symbol_code='000001', start_date='2021-01-04')` returns a DataFrame with ≥ 1,100 rows. `MarketReader.get_index_klines(symbol_code='000300.SH', start_date='2021-01-04')` returns ≥ 1,100 rows. `MarketReader.get_available_pairs()` returns ≥ 800 stock codes. `MarketReader.get_available_indices()` returns ≥ 8 indices including the 3 new ones. `MarketReader.get_trade_calendar(start_date='2021-01-04')` returns ≥ 1,200 dates. Fail: any of the above queries returns empty or insufficient data, or raises a `FileNotFoundError` or SQLite error.
Evidence: From the quant-dashboard project root, run:
```python
from src.data_layer.market_reader import MarketReader
mr = MarketReader()
assert len(mr.get_stock_klines('000001', start_date='2021-01-04')) >= 1100
assert len(mr.get_index_klines('000300.SH', start_date='2021-01-04')) >= 1100
assert len(mr.get_available_pairs()) >= 800
indices = mr.get_available_indices()
assert len(indices) >= 8
index_codes = {i['code'] for i in indices}
assert {'000300.SH', '000905.SH', '000852.SH'}.issubset(index_codes)
assert len(mr.get_trade_calendar(start_date='2021-01-04')) >= 1200
```
All assertions must pass. Also verify that `MarketReader`'s read-only connection (`?mode=ro`) does not conflict with active backfill writes (SQLite WAL mode allows concurrent readers).
